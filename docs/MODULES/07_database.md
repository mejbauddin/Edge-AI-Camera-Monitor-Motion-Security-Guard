# Phase 7: Database Module

SQLite-backed persistence with automatic migrations.

## Tables

`users`, `authorized_faces`, `unknown_faces`, `events`, `threats`, `recordings`, `snapshots`, `logs`, `settings`, `analytics`

## Tests

```powershell
D:\CyberSentinelX-build\tests\Release\csx_database_tests.exe
```
